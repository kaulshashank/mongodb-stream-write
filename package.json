{
  "name": "mongodb-stream-write",
  "version": "1.0.0",
  "description": "Make a writable stream to a MongoDB collection",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:w": "tsc -w",
    "prepublish": "npm run build",
    "test": "mocha --require ts-node/register --color --timeout 10000 --recursive './src/*.test.ts'"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:kaulshashank/mongodb-stream-write.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^4.1.3"
  },
  "devDependencies": {
    "@types/chai": "^4.2.22",
    "@types/mocha": "^9.0.0",
    "chai": "^4.3.4",
    "mocha": "^9.1.2",
    "mongodb-memory-server": "^7.4.3",
    "ts-node": "^10.3.0",
    "typescript": "^4.4.3"
  }
}
